import { Character } from 'libs/utils/Character'
import { Pair } from 'libs/utils/Pair'

export const round: Pair<Character> = { left: '(', right: ')' }

export const curly: Pair<Character> = { left: '{', right: '}' }

export const square: Pair<Character> = { left: '[', right: ']' }
